import 'package:flutter/material.dart';

class infst extends StatelessWidget {
  const infst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
/*  Container _infstw (int dan, String dat){
    return Container(
      child: Column(
        children: [
          Container(
            child: Text(dat),
          ),
          Container(
            child: Text("$dan"),
          )
        ],
      ),

    );*/
}
